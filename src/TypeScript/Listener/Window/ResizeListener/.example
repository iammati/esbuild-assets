Application.getContainer().get('LoadEvent').addListener(() => {
    console.log('Load event triggered as listener');

    Application.getContainer().get('ResizeEvent').addListener(() => {
        console.log('Resize event triggered as listener');
    });
});
