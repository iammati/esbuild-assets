export default class QueryParameters {
    call = (hook, next) => {
        // console.log('QPM called');

        // const queryParams = [];
        // const searchParams = new URLSearchParams(location.search);

        // for (const pair of searchParams.entries()) {
        //     let key = pair[0];
        //     let value = pair[1];

        //     queryParams[key] = value;
        // }

        // next();
    }
}
